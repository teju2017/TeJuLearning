########## This is a YAML file for loading ###################

---
environment: production
classes:
  nfs::server:
    exports:
      - /srv/share1
      - /srv/share3
parameters:

new parameter:branch 2        
new parameters:branch1
